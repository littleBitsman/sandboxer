# example-resource-management

Demonstrates how to manage `Instance`s (both those created and those accessed by the sandbox) and `RBXScriptConnection`s created by the sandbox as well as their semantics.